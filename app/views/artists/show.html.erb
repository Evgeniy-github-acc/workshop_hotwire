<%= render partial: 'header', locals: { artist: }  %>

<hr class="my-4">

  <%= render partial: "popular", locals: { tracks:, artist:, expand_link: true } %>

<hr class="my-4"/>

<%= render partial: "discography", locals: {artist:, albums:} %>
